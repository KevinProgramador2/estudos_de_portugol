programa {
  funcao inicio() {
   const inteiro MAX_NOTAS = 4
   inteiro notas[MAX_NOTAS]

   para(inteiro i = 0; i < MAX_NOTAS; i++){
    escreva("escreva a notas: ")
    leia(notas[i])
   }
    escreva("escreva a notas: ","\n")
    para (inteiro i = 0; i < MAX_NOTAS; i++){
      escreva(notas[i], "\n")
    }
   
  }
}
